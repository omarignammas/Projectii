# Backend origin (no trailing slash, no /api/v1 suffix).
# Local dev works fine without this set — it defaults to http://localhost:8080.
# In Netlify, set this to your deployed Render backend URL, e.g. https://projectii-backend.onrender.com
VITE_API_URL=http://localhost:8080
